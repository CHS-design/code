作为仓库级全局忽略清单，按语义分块组织：OS/编辑器（.DS_Store、Thumbs.db、.vscode/* 但显式保留 extensions.json）、本地环境与密钥（.env*、*.local）、Python 生态（__pycache__、pytest_cache、mypy_cache、ruff_cache、venv/env）、Jupyter、构建输出（_build/**）、机器学习产物（data/**、data_me、*.pth/*.pt/*.ckpt/*.onnx/*.h5/*.npy/*.npz/*.pkl/*.joblib）、压缩归档（*.zip/*.rar/*.7z/*.tar*/*.gz）以及日志临时文件（*.log/*.tmp/*.temp/~$*）。该文件是纯声明式配置，无代码逻辑。