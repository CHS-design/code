# LESSONS.md

- 周报修改场景中，用户说“写”“改”“补充”时，默认是写入现有周报 DOCX，而不是只在聊天中给草稿；除非用户明确要求只给文本。
- 用户已明确要求：以后周报不用进行可视化渲染，默认只做结构校验和内容位置检查。
- 新增周报条目时，不能只设置 `Normal` 或 `Heading 2` 样式；要复制相邻已有段落的直接格式、悬挂缩进、Tab 和字体字号，避免上下格式不统一。
